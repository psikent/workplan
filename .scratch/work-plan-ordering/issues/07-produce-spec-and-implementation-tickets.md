# 形成排序需求规格与实施票据

Type: task
Status: resolved
Blocked by: 06

## Question

把地图中已经闭合的全部决策整理为 `.scratch/work-plan-ordering/spec.md`，再按本地票据规范切分有依赖关系、可独立验收的实施票据，并提交用户批准；在获得明确批准前不实现。

## Answer

- 已形成 `../spec.md`，覆盖领域边界、统一查询流水线、排期顺序、各读取场景、半开时间边界、可排序字段、Web 偏好、游标契约、实时并发语义、XLS、`sortOrder` 退役、性能预算、验收矩阵及上线回退。
- 已生成十张有依赖关系的实施票据 `08–17`：
  - 08 先原型验证自然排序键和 SQLite 查询计划，避免无法部署的 collation 假设。
  - 09–10 建立共享契约和服务端统一查询/游标。
  - 11–13 迁移工作台、提醒、Web 页面和 XLS。
  - 14 启动 Work Plan `sortOrder` 兼容退役。
  - 15 完成回归、性能和发布候选验收。
  - 16 为需要单独生产授权和十四天观察的 `ready-for-human` 票据。
  - 17 只在观察放行后删除数据库遗留。
- 08–15 与 17 标记为 `ready-for-agent`，16 标记为 `ready-for-human`；依赖链阻止数据库删列早于生产观察。
- 当前只完成规划文档，没有修改应用代码、数据库或生产环境。下一步必须由用户审核并明确批准 `../spec.md` 与票据后，才能从 08 开始实施。

## Comments

- 2026-09-03：规格和实施票据已形成并提交用户审批。
