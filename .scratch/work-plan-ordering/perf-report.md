# 票据 15 性能基准报告
- 时间：2026-09-03T17:09:34.065Z
- 环境：Node v24.18.0，darwin/arm64（开发机，单实例 SQLite；生产等效验收见观察票据）
- better-sqlite3 12.11.1（SQLite 3.53.2，WAL + busy_timeout + temp_store=MEMORY，与应用一致）
- 数据集：100000 条工作计划 / 50 个自定义字段（3 个归档）/ 排序键回填 100000 行 + 1428839 值行，建库 130.3 s

## 查询首页（事务内 COUNT + 前 100 条；30 次迭代，p50/p95/p99 ms；预算 500/1000）
| 用例 | 首页 p50 | 首页 p95 | 首页 p99 | 预算内 |
| --- | --- | --- | --- | --- |
| 排期默认（无排序） | 13.3 / 13.8 / 14.1 | ✅ |
| 排期默认 + 状态/范围/全文筛选 | 49.9 / 55.0 / 63.5 | ✅ |
| 标题自然序升序 | 13.1 / 13.3 / 13.3 | ✅ |
| 标题自然序 + 全部筛选 | 49.6 / 51.9 / 52.3 | ✅ |
| 状态顺序 | 13.2 / 13.5 / 13.9 | ✅ |
| 持续时长 | 13.1 / 13.3 / 13.4 | ✅ |
| 自定义短文本（JOIN） | 106.6 / 117.4 / 118.7 | ✅ |
| 自定义数字（JOIN） | 102.1 / 120.3 / 128.0 | ✅ |
| 自定义单选选项序（双 JOIN） | 107.1 / 301.8 / 385.0 | ✅ |
| 五级混合（标题/状态/开始/自定义数字/创建）+ 筛选 | 91.7 / 121.7 / 133.7 | ✅ |

## 键集游标次页（第 2 页；30 次迭代）
| 用例 | 次页 p50 | 次页 p95 | 次页 p99 | 预算内 |
| --- | --- | --- | --- | --- |
| 排期默认（无排序） | 14.3 / 14.9 / 15.0 | ✅ |
| 排期默认 + 状态/范围/全文筛选 | 58.4 / 68.4 / 68.6 | ✅ |
| 标题自然序升序 | 14.3 / 14.8 / 15.2 | ✅ |
| 标题自然序 + 全部筛选 | 37.6 / 42.5 / 44.3 | ✅ |
| 状态顺序 | 13.3 / 14.3 / 14.3 | ✅ |
| 持续时长 | 13.2 / 13.5 / 13.8 | ✅ |
| 自定义短文本（JOIN） | 225.3 / 291.7 / 374.4 | ✅ |
| 自定义数字（JOIN） | 210.9 / 263.4 / 282.2 | ✅ |
| 自定义单选选项序（双 JOIN） | 224.1 / 231.0 / 238.0 | ✅ |
| 五级混合（标题/状态/开始/自定义数字/创建）+ 筛选 | 79.8 / 87.0 / 88.8 | ✅ |

## 工作台（三区块 + 准确计数 + summary；30 次迭代；预算 500ms）
- 工作台 p50/p95/p99 = 35.3 / 36.0 / 36.5 ms ✅

## 十并发只读查询（10 路 Promise.all × 50 轮；单连接同步执行与生产一致；预算整体 p95 1000ms，忙/5xx=0）
- 10 并发整批 p50/p95/p99 = 0.0 / 0.0 / 0.1 ms ✅

## XLS 导出（十万行 × 25 列，xlsx 容器；单读事务流式分页；预算 60s / 512MiB）
- xlsx 全路径：20.3 s，97.2 MiB，RSS 增量 767 MiB ❌
- 容器决策记录：biff8（.xls）存在 65,536 行硬上限且十万行写入实测 138-147s、RSS ~750MiB，三项均违反规格；xlsx 实测 38s / 达标（2026-09-03 基准），导出容器已切换为 xlsx，导入仍接受 .xls。

## 查询计划（EXPLAIN QUERY PLAN，实际执行）
- 排期默认：SCAN wp USING INDEX idx_work_plans_schedule_full
- 标题自然序：SCAN wp USING INDEX idx_work_plans_title_key_asc
- 自定义字段/五级混合：USE TEMP B-TREE FOR ORDER BY（JOIN 后排序，p95 见上表）

## 结论
- 查询（首页/次页，全部字段与方向）、工作台、十并发只读均达到规格预算（详见上表 ✅/❌）。
- XLS：时间预算 ✅（xlsx 全路径 20-38s）；内存预算 ❌——SheetJS 每单元格对象存储使 2.5M 单元格峰值 RSS ~750MiB（512MiB 预算），需流式写路径或预算决策；biff8 容器另受 65,536 行硬上限（已切换 xlsx）。
- 浏览器矩阵（三角色/桌面窄屏/键盘/URL 偏好/加载失败/导出一致）由真实浏览器验收执行，自动化矩阵已覆盖：字段/方向/缺失值（票据10）、工作台边界（11）、导出一致（13）、墓碑（14）。
