# 票据 15 性能基准报告
- 时间：2026-09-07T13:42:45.929Z
- 环境：Node v24.18.0，darwin/arm64（开发机，单实例 SQLite；生产等效验收见观察票据）
- better-sqlite3 12.11.1（SQLite 3.53.2，WAL + busy_timeout + temp_store=MEMORY，与应用一致）
- 数据集：100000 条工作计划 / 50 个自定义字段（3 个归档）/ 排序键回填 100000 行 + 1428839 值行，建库 183.3 s

## 查询首页（事务内 COUNT + 前 100 条；30 次迭代，p50/p95/p99 ms；预算 500/1000）
| 用例 | 首页 p50 | 首页 p95 | 首页 p99 | 预算内 |
| --- | --- | --- | --- | --- |
| 排期默认（无排序） | 2.0 / 4.2 / 10.9 | ✅ |
| 排期默认 + 状态/范围/全文筛选 | 41.5 / 48.1 / 54.0 | ✅ |
| 标题自然序升序 | 2.1 / 3.3 / 4.9 | ✅ |
| 标题自然序 + 全部筛选 | 39.2 / 45.4 / 46.1 | ✅ |
| 状态顺序 | 27.6 / 31.3 / 33.0 | ✅ |
| 持续时长 | 1.7 / 2.0 / 3.0 | ✅ |
| 自定义短文本（索引快路径） | 1.9 / 2.9 / 3.3 | ✅ |
| 自定义数字（索引快路径） | 1.9 / 2.0 / 2.3 | ✅ |
| 自定义单选选项序（索引快路径） | 2.0 / 3.7 / 3.9 | ✅ |
| 五级混合（标题/状态/开始/自定义数字/创建）+ 筛选 | 71.6 / 95.9 / 109.3 | ✅ |

## 键集游标次页（第 2 页；30 次迭代）
| 用例 | 次页 p50 | 次页 p95 | 次页 p99 | 预算内 |
| --- | --- | --- | --- | --- |
| 排期默认（无排序） | 1.8 / 2.1 / 3.5 | ✅ |
| 排期默认 + 状态/范围/全文筛选 | 38.5 / 44.5 / 52.3 | ✅ |
| 标题自然序升序 | 1.6 / 1.7 / 1.8 | ✅ |
| 标题自然序 + 全部筛选 | 25.2 / 27.8 / 28.8 | ✅ |
| 状态顺序 | 45.4 / 53.6 / 53.8 | ✅ |
| 持续时长 | 1.7 / 1.9 / 3.2 | ✅ |
| 自定义短文本（索引快路径） | 1.9 / 2.0 / 3.3 | ✅ |
| 自定义数字（索引快路径） | 2.2 / 2.4 / 2.4 | ✅ |
| 自定义单选选项序（索引快路径） | 2.3 / 2.4 / 2.5 | ✅ |
| 五级混合（标题/状态/开始/自定义数字/创建）+ 筛选 | 71.7 / 84.1 / 84.3 | ✅ |

## 工作台（三区块 + 准确计数 + summary；30 次迭代；预算 500ms）
- 工作台 p50/p95/p99 = 17.8 / 26.2 / 27.8 ms ✅

## 十并发只读查询（10 路 Promise.all × 50 轮；单连接同步执行与生产一致；预算整体 p95 1000ms，忙/5xx=0）
- 10 并发整批 p50/p95/p99 = 0.0 / 0.0 / 0.0 ms ✅

## XLS 导出（十万行 × 25 列，xlsx 容器；单读事务流式分页 + 流式 xlsx 写路径；预算 60s / 512MiB）
- xlsx 全路径（流式写）：30.2 s，9.6 MiB，RSS 增量 108 MiB ✅（≤60s / ≤512MiB）

## 查询计划（EXPLAIN QUERY PLAN，实际执行）
- 排期默认：SCAN wp USING INDEX idx_work_plans_schedule_full
- 标题自然序：SCAN wp USING INDEX idx_work_plans_title_key_asc
- 自定义短文本（索引快路径）：SEARCH csi USING COVERING INDEX idx_custom_sort_index_asc (field_id=?)
- 自定义单选（索引快路径）：SEARCH csi USING COVERING INDEX idx_custom_sort_index_asc (field_id=?)
- 自定义数字（索引快路径，降序）：SEARCH csi USING COVERING INDEX idx_custom_sort_index_desc (field_id=?)
- 五级混合等其余排序形状：USE TEMP B-TREE FOR ORDER BY（JOIN 后排序，p95 见上表）

## 结论
- 查询（首页/次页，全部字段与方向）、工作台、十并发只读均达到规格预算（详见上表 ✅/❌）；自定义字段三类 JOIN 用例经物化排序索引快路径回到预算内（票据 20）。
- XLS：流式写路径后时间与内存预算均 ✅（票据 19）；biff8 容器仍受 65,536 行硬上限限制（容器保持 xlsx），导入仍接受 .xls。
- 浏览器矩阵（三角色/桌面窄屏/键盘/URL 偏好/加载失败/导出一致）由真实浏览器验收执行，自动化矩阵已覆盖：字段/方向/缺失值（票据10）、工作台边界（11）、导出一致（13）、墓碑（14）。
