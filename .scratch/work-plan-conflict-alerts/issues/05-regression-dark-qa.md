# 05 — 回归、暗色与端到端验收
Type: task
Status: ready-for-agent
Blocked by: 03, 04
Spec: ../spec.md
Scope: 全仓、浏览器 QA

## 背景

规格全部验收标准(1–7)的端到端核对,重点:明暗双主题、非冲突面回归、无写死面色。

## 改动清单

1. `pnpm run typecheck` 与全部测试(web + server)通过。
2. 浏览器 QA(亮/暗两主题,基于最新构建——多机开发,先确认构建与服务器一致):
   - 构造冲突数据(同 owner 区间相交、端点相接、A-B-C 传递链、completed/cancelled/空 owner 对照组);
   - 核对四处:甘特条整条警示色、列表行软底、浮动提示强制负责人行 + 冲突清单、抽屉边框 + 小字;
   - 非冲突回归:状态配色不变、tooltip 用户配置被尊重、表单报错仍为 coral、可正常保存冲突任务;
   - 冲突解除(改期/换人/完成)后提醒随刷新消失。
3. `styles.css` 写死面色扫描:本功能新增规则仅引用 token。
4. spec Status 更新为「已实现」并记录验证结论。

## 验收

- 规格验收标准 1–7 全部核对通过;QA 截图(明暗)留档。
