# 03 — 回归与真实浏览器验收
Type: task
Status: resolved
Blocked by: 01, 02
Spec: ../spec.md
Scope: 全仓测试、真实浏览器 QA、规格与票据结案

## 背景

本票验证候选标记、现有冲突详情和持久化全局标记使用同一语义，并检查原生下拉在浏览器、主题和窄屏中的实际可读性。

## 改动清单

1. 运行 contracts、server、web 的定向测试及全仓 `pnpm run typecheck`、`pnpm test`。
2. 构造隔离数据矩阵：同 owner 多项重叠、端点相接、毫秒重叠、different owner、completed/cancelled、空 owner、编辑自身、automatic/manual、单个周期 Occurrence。
3. 真实浏览器覆盖：桌面与 390px 窄屏、亮色与暗色、新建与编辑；检查展开 option 文案、选中态文案、数量与详情一致、Option Order、键盘选择及保存。
4. 验证修改时间/状态后的防抖刷新、30 秒定期刷新、迟到响应保护、失败提示和成功恢复；冲突期间始终可保存。
5. 回归既有四处 Owner Conflict 提醒：列表、甘特条、甘特 tooltip、抽屉选中后详情；确认查询范围/筛选/分页外 Counterpart 仍不会漏报。
6. 记录测试数量、浏览器矩阵、截图/证据与发现的问题；全部通过后将 spec 和三个票据状态更新为已完成。

## 验收

- 规格验收标准 1–9 均有自动化或浏览器证据。
- 不把结构测试或组件模拟当作原生 select 在真实浏览器中的最终视觉验收。

## Answer

已完成全仓回归与真实浏览器验收。浏览器使用 Codex In-app Browser，在隔离临时数据库中创建负责人字段和重叠计划，覆盖桌面/390×844 窄屏、亮色/暗色、新建/编辑、选项数量、选中详情、可保存、API 中断错误提示及恢复。最终页面 URL 为 `http://127.0.0.1:5173/work-plans`，标题为「工作计划」，浏览器最终错误/警告日志为空；截图通过浏览器工具展示且未写入仓库。

验证证据（2026-09-10）：contracts 25/25、server 235/235、web 339/339、脚本 46/46（9 个平台跳过项）；全仓 typecheck 通过；代码审查无 P0/P1。

## Comments

- 真实 QA 使用的测试服务、管理员和数据均位于系统临时目录，已在验收后停止；正式工作区数据未被改动。
