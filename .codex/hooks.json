{
  "description": "WorkPlan automatic release after each Codex turn; requires codex_hooks enabled, code-reviewer approval for dirty changes, and the local .zcode release hook.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$(git rev-parse --show-toplevel)/.codex/hooks/auto-release-codex.sh\"",
            "timeout": 1200,
            "statusMessage": "回合结束 → 验收、审查后自动提交、推送并发布 hk3"
          }
        ]
      }
    ]
  }
}
